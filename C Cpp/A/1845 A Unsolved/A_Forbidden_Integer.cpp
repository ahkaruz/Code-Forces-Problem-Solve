#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>a;
    int n = 10,
        k = 3,
        x = 1;
    for (int i = k; i >= 1; i--)
    {
        if (i == x)
            continue;
        
    }

    return 0;
}